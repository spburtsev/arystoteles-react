// import { createSlice } from '@reduxjs/toolkit';

// const initialState = {
//   availableSections: [],
//   currentSection: null,
// };

// const profileSlice = createSlice({
//   name: 'profile',
//   initialState: initialState,
//   reducers: {},
// });

// export const localeActions = profileSlice.actions;

// export const selectLocale = (state) => state.locale.localization;

// export default profileSlice;
